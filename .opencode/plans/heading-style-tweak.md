# Heading style refresh

## File: `_layouts/default.html`

**Line 315** — Replace the existing `.page-content h2` rule with:

```css
    .page-content h2 { font-family: 'Oswald', sans-serif; font-weight: 500; font-size: clamp(34px, 5vw, 50px); color: #f4f5f7; margin-bottom: 16px; letter-spacing: 0.06em; text-shadow: 2px 2px 0 rgba(0,0,0,0.35); border-left: 4px solid #7440a3; padding-left: 16px; }
```

That's it — one line change. Everything else stays as-is.
